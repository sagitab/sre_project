CONNECT to DB 

mysql -h 127.0.0.1 -P 4000 -u root

use app_db;

update users set username = "sagi" where id = 1;

select * from users;


setup commands

docker-compose up --build -d

Run this command in your terminal. This tells TiCDC to watch all changes and send them to the Kafka broker 
inside your Docker network:

 MSYS_NO_PATHCONV=1 docker exec -it ticdc /cdc cli changefeed create \
--server=http://127.0.0.1:8300 \
--sink-uri="kafka://kafka:29092/ti-db-updates?protocol=canal-json" \
--changefeed-id="tidb-to-kafka"

docker-compose restart node-consumer